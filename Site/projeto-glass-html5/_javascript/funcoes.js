// Função JavaScript que muda a imagem do ícone quando o mouse passa por cima de um item no menu
function mudaFoto(foto) {
    document.getElementById("icone").src = foto; // Altera a imagem do elemento com id "icone"
}